# ClickZone Web POS (clean build)

This is the **new cloud-first PWA** build of the ClickZone POS — a fresh Vite + React + TS + Tailwind + shadcn app on **Supabase only** (no Electron, no Express, no SQLite).

- Legacy Electron source lives one level up in `../` (read-only reference — port from `../src`, do not run it).
- Build plan: `../CLICKZONE_WEB_BUILD_PLAN.md`
- Phase-by-phase runbook: `../IMPLEMENTATION_RUNBOOK.md`
- Client issues + analysis: `../.handoff/03-CLIENT-ISSUES.md`

Build order is in the runbook. This folder starts at **Phase 1 — skeleton**.
